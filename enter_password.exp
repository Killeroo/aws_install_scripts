#!/usr/bin/expect
set timeout 10
set password [lindex $argv 0]
#set command [lindex $argv 1]

spawn sudo mysql -u root -p -e "SHOW STATUS LIKE 'wsrep_cluster_size'"

expect "Enter password:" {send "password\r"}